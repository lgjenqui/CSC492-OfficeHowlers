import Box from "@mui/material/Box";

interface Props {}

const CourseDetailsAssistant = ({}: Props) => {
  return (
    <Box
      sx={{
        width: "85%",
        height: "100%",
        mt: 0,
      }}
    >
      Assistant view
    </Box>
  );
};

export default CourseDetailsAssistant;
