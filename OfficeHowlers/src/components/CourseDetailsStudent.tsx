import Box from "@mui/material/Box";

interface Props {}

const CourseDetailsStudent = ({}: Props) => {
  return (
    <Box
      sx={{
        width: "85%",
        height: "100%",
        mt: 0,
      }}
    >
      Student view
    </Box>
  );
};

export default CourseDetailsStudent;
